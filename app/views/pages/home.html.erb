<h1>Planr App</h1>
<p>Witamy!</p> 


<% if user_signed_in? %>
	<%= link_to "Baza informacji", posts_path %>  
	<%= @user.name %> 
	<%= image_tag(@picture, :size => "32x32") %>
<% else %>        
	Załóż konto i zaloguj się by zobaczyć tajną bazę danych.
<% end %>
</div>
